import React, {Fragment} from "react";
import {
    Container
} from "react-bootstrap";

export default class Comments extends React.Component {
    render() {
        return(
            <Fragment>
                <Container className="gk-container">
                Esto es una prueba de Comments
                </Container>
            </Fragment>
            );
    }
}